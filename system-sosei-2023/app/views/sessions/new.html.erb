<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>ログイン</title>
</head>
<body class="bg-gray-100">

<div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full bg-white p-6 rounded-lg shadow-md">
        <div>
            <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
                ログインする
            </h2>
        </div>

        <%= form_with(url: login_path, scope: :session, local: true, html: {class: "mt-8 space-y-6"}) do |f| %>
            <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
            <div class="rounded-md shadow-sm -space-y-px">
                <div>
                    <%= f.label :email, class: 'sr-only' %>
                    <%= f.email_field :email, id: "email", required: true, class: 'appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-t-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm', placeholder: 'メールアドレス' %>
                </div>
                <div>
                    <%= f.label :password, class: 'sr-only' %>
                    <%= f.password_field :password, id: "password", required: true, class: 'appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-b-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm', placeholder: 'パスワード' %>
                </div>
                <div>
                    <%= f.submit "ログイン", class: "group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
                </div>
            </div>

            <div class="mt-6 text-center">
                <p>アカウントを持っていませんか？ <%= link_to '作成する', signup_path, class: "text-indigo-600 hover:text-indigo-500" %></p>
            </div>
        <% end %>
    </div>
</div>

</body>
</html>
